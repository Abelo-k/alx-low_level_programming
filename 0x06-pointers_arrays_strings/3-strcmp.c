#include "main.h"
/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 *
 * Return: @s1 - @s2
 */
int _strcmp(char *s1, char *s2)
{
	int a, i;

	for (i = 0; s1[i] != '\0' && s2[i] != '\0'; i++)
	{
		a = (s1 - s2);
	}
	return (a);
}
